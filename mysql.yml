- name: installing mysl server
  hosts: mysql
  become: yes
  tasks:
   - name: installing mysql-server
     ansible.builtin.package:
       name: mysql-server
       state: present

   - name: starting service
     ansible.builtin.service:
       name: mysqld
       state: started
       enabled: yes




   
      